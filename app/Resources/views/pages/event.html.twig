{% extends 'section/index/index.html.twig' %}

{% block content %}
    <div id="wrapper">
        {% if event is empty %}
            <div class="error">Erreur: l'évènements d'id {{ id }} n'existe pas!</div>
        {% else %}
            <div id="container">
                <h2>{{ event.nom }} - du {{ event.debut|date('Y m d H\\hi') }} à {{ event.fin|date('H\\hi') }}</h2>
{#                <p>Catégorie: {{ categorie.nom }}</p>#}
{#                <p><img src="{{asset(image.url)}}" alt="image.alt" />#}
                {{ event.description }}</p>
            </div>
        {% endif %}
    </div>
{% endblock %}